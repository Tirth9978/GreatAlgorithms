int left = 0, right = n-1;
while(left <= right){
    if(canMerge(left, right)){
        // Process and move pointers
    }
    // Decide which pointer to move based on condition
    // Often: move the "worse" one
}